挂单：
1. 后端接口， 成功--》合约（dm5nft合约的  isaproveAll），返回 false 调 setApproveAll
// 注意：
  出售按钮点击先调后台:
    1 ---成功出现步骤提示弹窗， 点击确定走合约（是/否授权，关闭/授权）。
    2 ---失败直接将错误展示给用户


交易：
调后台接口，成功
  1 --- 返回tokenid, 卖家地址， 合约地址, nft价格（value），币的类型（1是 usdt, 2 其他）参数，
  2 --- 调 callTransferFrom 方法，成功返回 hash， 
  3 --- 然后调后端确认上链接口， 将(hash，tokenid， 卖家地址) 传给后端



问题：

2. 出售，，转增 ---- 提示信息 duration = 0

9. 购买成功之后，调 detail ，取交易列表




4. 创建item 没有调接口，回显
5. 创建 item 的 提示信息（和 collection  不一样）
6. 转增之后，接受方没收到
8. 出售弹窗 样式修改
10. item, collection 尾部链接非必填